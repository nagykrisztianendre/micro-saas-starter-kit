# Teams module notes

Teams/organizations routes are included as UI placeholders:
- `/organizations`
- `/organizations/[id]/members`
- `/organizations/[id]/invite`

Use these pages as boundaries for adding:
- organization persistence
- invites with token validation
- role enforcement
- membership audit events
